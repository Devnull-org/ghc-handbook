node_modules/
dist/
.astro/

# GHC lives at vendor/ghc as a submodule and IS tracked (as a commit pointer).
# What must never be committed is what building it leaves behind in there.
vendor/ghc/_build/
vendor/ghc/_validatebuild/

# Scratch output from dump generation before it is promoted into data/
.dump-tmp/

.DS_Store
*.log
